//fetchProduit(); // le fetch fonctionne. on va créer une autre fonction qui va utiliser cette fonction.













//--------------------------------------------


















/*
const showNounours = async () => {
	await fetchApi2();

	results.innerHTML = (

		gentil
			.map(pelushe => (
				`
					<div class="article-item">
							<h3 class="article-name">${pelushe.name}</h3>					
					</div>
				`
			)).join('')
	);
};

showNounours();*/